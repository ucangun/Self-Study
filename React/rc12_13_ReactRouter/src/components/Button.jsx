import React from "react";

const Button = () => {
  return (
    <div>
      <button>LOGIN</button>
      <button>OPEN</button>
      <button>CLOSE</button>
    </div>
  );
};

export default Button;
